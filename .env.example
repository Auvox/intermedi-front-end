# API de dados usada pelo navegador (reinicie o Vite ao alterar).
VITE_API_BASE_URL=http://localhost:3000
# API de dados usada pelo servico de autenticacao Node.
# Configure esta variavel no ambiente do processo Node.
API_BASE_URL=http://localhost:3000
